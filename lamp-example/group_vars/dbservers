---
# Variables for the dbservers group.
# These are automatically loaded for hosts in the dbservers group — no vars_files needed.
#
# NOTE: In production, store sensitive values like upassword in an Ansible Vault file.
# Example: ansible-vault encrypt_string 'mysecret' --name 'upassword'

mysqlservice: mariadb
mysql_port: 3306
dbuser: foouser
dbname: foodb
upassword: abc
